#include <stdio.h>
#include <unistd.h>
#include <sys/wait.h>

int main()
{
    int pid = fork();

    if (pid == 0)
    {
        printf("Child process running...\n");
        printf("Child PID: %d\n", getpid());

        return 0;
    }
    else
    {
        printf("Parent process running...\n");
        printf("Parent PID: %d\n", getpid());

        sleep(20);

        printf("Parent finished\n");
    }

    return 0;
}
