This practical shows how Linux creates and manages processes.
We use fork() to create a child, execlp() to run a command, and wait() so the parent waits for the child to finish.

It also uses commands like ps, top, lscpu, and lsblk to understand the system and its hardware.
